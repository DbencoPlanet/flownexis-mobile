# Strict requirement: Base URLs must be loaded from env.
# Note: If testing on a physical device, replace 'localhost' with your computer's local Wi-Fi IP address (e.g., 192.168.1.100).
# If using an Android Emulator, use '10.0.2.2' instead of 'localhost'.
# PUBLIC_API_BASE_URL="http://localhost:5005/api/v1"
PUBLIC_API_BASE_URL="http://10.131.234.91:5005/api/v1"